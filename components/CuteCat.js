import { Text } from 'react-native';

const CuteCat = () => (
  <Text style={{
    fontSize: 30,
    fontWeight: '600',
    textAlign: 'center',
    paddingHorizontal: 20,
  }}>🐱</Text>
);

export default CuteCat;
